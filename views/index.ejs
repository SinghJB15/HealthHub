<%- include("partials/head.ejs") %>
<body>
    <%- include("partials/header.ejs") %>
    

    <div class="container">
        <ul>
            <% for (let i = 0; i < topics.length; i++) { %>
                <li>
                    <a href="/health/<%= encodeURIComponent(topics[i].name)%>">
                        <%= topics[i].name %>
                    </a>
                </li>
                <% } %>
        </ul>
    </div>
</body>
