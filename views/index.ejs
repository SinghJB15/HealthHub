<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HealthHub Index</title>
</head>
<body>
    <header>HealthHub</header>

    <nav>

    </nav>

    <div class="container">
        <ul>
            <% for (let i = 0; i < topics.length; i++) { %>
                <li>
                    <a href="/health/<%= encodeURIComponent(topics[i].name)%>">
                        <%= topics[i].name %>
                    </a>
                </li>
                <% } %>
        </ul>
    </div>
</body>
</html>s